name: HQ WorldMap Patch Payload

on:
  workflow_dispatch:

jobs:
  payload:
    runs-on: ubuntu-latest
    steps:
      - name: Apply WorldMap durability patch
        run: |
          python3 splat-native-ios/scripts/apply_worldmap_transition_patch.py
      - name: Commit patched source
        run: echo 'Commit is performed by the parent PR workflow.'
