name: APP2-002 Codemagic Diagnose

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  diagnose:
    if: github.actor == 'ALLSUNDAY1122' && github.event.issue.title == '[APP2-002] Diagnose Codemagic build'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Fetch and sanitize Codemagic build diagnostics
        env:
          CM_API_TOKEN: ${{ secrets.CM_API_TOKEN }}
        run: |
          python3 - <<'PY'
          import json, os, urllib.request, urllib.error
          from pathlib import Path
          build_id='6aaa38d60ecca8f9a7c71eb8'
          app_id='6a66134ea9f671d548da335a'
          token=os.environ['CM_API_TOKEN'].strip()
          def get(url):
              req=urllib.request.Request(url,headers={'x-auth-token':token,'Accept':'application/json'},method='GET')
              try:
                  with urllib.request.urlopen(req,timeout=30) as r:
                      raw=r.read().decode('utf-8',errors='replace')
                      try: return json.loads(raw)
                      except Exception: return {'non_json':raw[:12000]}
              except urllib.error.HTTPError as e:
                  return {'http_status':e.code,'error':e.read().decode('utf-8',errors='replace')[:12000]}
          v3=get(f'https://codemagic.io/api/v3/builds/{build_id}')
          actions=get(f'https://codemagic.io/api/v3/builds/{build_id}/actions')
          listing=get(f'https://api.codemagic.io/builds?appId={app_id}')
          rows=listing.get('builds') or listing.get('data') or [] if isinstance(listing,dict) else []
          if isinstance(rows,dict): rows=rows.get('builds') or []
          exact=None
          for row in rows if isinstance(rows,list) else []:
              if str(row.get('_id') or row.get('id') or '')==build_id:
                  exact=row; break
          payload={'v3_build':v3,'actions':actions,'build_list_exact_match':exact,'build_list_meta':{'row_count':len(rows) if isinstance(rows,list) else None}}
          sensitive=('secret','token','password','credential','private','apikey','api_key','environment','variable')
          def clean(v):
              if isinstance(v,dict):
                  return {k:('[REDACTED]' if any(p in str(k).lower() for p in sensitive) else clean(x)) for k,x in v.items()}
              if isinstance(v,list): return [clean(x) for x in v]
              return v
          out=Path('automation/codemagic-results/app2-002-build9-latest-diagnostic.json')
          out.parent.mkdir(parents=True,exist_ok=True)
          out.write_text(json.dumps(clean(payload),ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          PY
      - name: Persist diagnostic
        run: |
          cp automation/codemagic-results/app2-002-build9-latest-diagnostic.json /tmp/app2-002-diagnostic.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin main
          git reset --hard origin/main
          mkdir -p automation/codemagic-results
          cp /tmp/app2-002-diagnostic.json automation/codemagic-results/app2-002-build9-latest-diagnostic.json
          git add automation/codemagic-results/app2-002-build9-latest-diagnostic.json
          git commit -m 'APP2-002: persist latest Build 9 diagnostic' || exit 0
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Report
        if: always()
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.issues.createComment({owner:context.repo.owner,repo:context.repo.repo,issue_number:context.issue.number,body:`APP2-002 Codemagic diagnostic finished. status=${{ job.status }}; run_id=${context.runId}`});
