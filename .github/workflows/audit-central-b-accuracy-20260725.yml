name: Audit Central B Accuracy 20260725

on:
  pull_request:
    branches: [region/central]

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: audit-central-b-accuracy-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  audit-round:
    if: github.head_ref == 'quality/central-b-accuracy-audit-20260725'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply Round 6 confirmed corrections
        working-directory: jichitai-compare
        run: |
          if node --input-type=module <<'NODE'
          import { readFile } from 'node:fs/promises';
          const audit = JSON.parse(await readFile('operations/audits/central-b-accuracy-audit-20260725.json', 'utf8'));
          const round = audit.batches.find((item) => item.round === 6);
          process.exit(round?.status === 'completed' ? 1 : 0);
          NODE
          then
            node scripts/fix-central-b-accuracy-round6.mjs apply
          else
            echo 'Round 6 corrections already finalized.'
          fi
      - name: Regenerate and validate municipality data
        working-directory: jichitai-compare
        run: |
          npm run generate
          npm run progress
          npm run validate:generated
      - name: Audit current Central B batch
        working-directory: jichitai-compare
        run: node scripts/audit-central-b-accuracy-round.mjs
      - name: Upload audit round result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: central-b-accuracy-round-result
          path: jichitai-compare/operations/audits/central-b-accuracy-round-output.json
          if-no-files-found: error
          retention-days: 14
      - name: Finalize Round 6 audit record
        working-directory: jichitai-compare
        run: |
          if node --input-type=module <<'NODE'
          import { readFile } from 'node:fs/promises';
          const audit = JSON.parse(await readFile('operations/audits/central-b-accuracy-audit-20260725.json', 'utf8'));
          const output = JSON.parse(await readFile('operations/audits/central-b-accuracy-round-output.json', 'utf8'));
          const round = audit.batches.find((item) => item.round === 6);
          process.exit(output.round === 6 && output.errorCount === 0 && round?.status !== 'completed' ? 0 : 1);
          NODE
          then
            node scripts/fix-central-b-accuracy-round6.mjs finalize
          else
            echo 'Round 6 finalization is not required.'
          fi
      - name: Normalize Round 6 source changes
        working-directory: jichitai-compare
        run: |
          node --input-type=module <<'NODE'
          import { readFile, writeFile } from 'node:fs/promises';
          const codes = ['29425','29427','29441','29442','29443'];
          for (const code of codes) {
            const municipalityPath = `data/municipalities/29/${code}.json`;
            const municipality = JSON.parse(await readFile(municipalityPath, 'utf8'));
            await writeFile(municipalityPath, JSON.stringify(municipality) + '\n', 'utf8');
            const taskPath = `operations/tasks/${code}.json`;
            const task = JSON.parse(await readFile(taskPath, 'utf8'));
            await writeFile(taskPath, JSON.stringify(task) + '\n', 'utf8');
          }
          NODE
      - name: Commit Round 6 audit results
        run: |
          git restore --source=origin/region/central -- \
            jichitai-compare/data/generated/municipalities.json \
            jichitai-compare/operations/progress.json
          rm -f jichitai-compare/operations/audits/central-b-accuracy-round-output.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            jichitai-compare/data/municipalities/29/29425.json \
            jichitai-compare/data/municipalities/29/29427.json \
            jichitai-compare/data/municipalities/29/29441.json \
            jichitai-compare/data/municipalities/29/29442.json \
            jichitai-compare/data/municipalities/29/29443.json \
            jichitai-compare/operations/tasks/29425.json \
            jichitai-compare/operations/tasks/29427.json \
            jichitai-compare/operations/tasks/29441.json \
            jichitai-compare/operations/tasks/29442.json \
            jichitai-compare/operations/tasks/29443.json \
            jichitai-compare/operations/audits/central-b-accuracy-audit-20260725.json
          if git diff --cached --quiet; then
            echo 'No Round 6 changes to commit.'
          else
            git commit -m 'audit: 中日本B第6回の誤調査を訂正'
            git push origin HEAD:${{ github.head_ref }}
          fi
