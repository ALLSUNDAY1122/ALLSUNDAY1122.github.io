name: Temporary North B Fiscal Phase2 Fixes
# rerun: 2026-07-25T13:18:00+09:00

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/temp-north-b-fiscal-phase2-fixes.yml'

permissions:
  contents: write

concurrency:
  group: north-b-fiscal-phase2-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout audit branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Apply phase2 corrections
        run: node jichitai-compare/scripts/temp-north-b-fiscal-phase2-fixes.mjs

      - name: Regenerate and validate
        working-directory: jichitai-compare
        run: |
          npm run generate
          npm run progress
          npm run validate:generated

      - name: Commit phase2 corrections
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add jichitai-compare/data/municipalities \
                  jichitai-compare/operations/tasks \
                  jichitai-compare/operations/audits/north-b-fiscal-anyone-triage-20260725.json \
                  jichitai-compare/operations/control/session-checkpoints/north-b.json \
                  jichitai-compare/data/generated \
                  jichitai-compare/operations/progress.json \
                  jichitai-compare/municipality \
                  jichitai-compare/sitemap.xml
          if git diff --cached --quiet; then
            echo 'No phase2 changes to commit.'
            exit 1
          fi
          git commit -m 'fix: 北日本B旧年度候補第2段階の確定不整合を修正 [skip ci]'
          git pull --rebase origin "${{ github.head_ref }}"
          git push origin HEAD:"${{ github.head_ref }}"
