name: Temporary North B Anyone Multiple Gap Fixes

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/temp-north-b-anyone-multiple-gap-fixes.yml'

permissions:
  contents: write

concurrency:
  group: north-b-anyone-multiple-gap-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout audit branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Apply confirmed corrections
        run: node jichitai-compare/scripts/temp-north-b-anyone-multiple-gap-fixes.mjs

      - name: Regenerate and validate
        working-directory: jichitai-compare
        run: |
          npm run generate
          npm run progress
          npm run validate:generated

      - name: Commit corrected data and generated outputs
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add jichitai-compare/data/municipalities \
                  jichitai-compare/operations/tasks \
                  jichitai-compare/operations/audits/north-b-anyone-multiple-gap-review-20260725.json \
                  jichitai-compare/operations/control/session-checkpoints/north-b.json \
                  jichitai-compare/data/generated \
                  jichitai-compare/operations/progress.json \
                  jichitai-compare/municipality \
                  jichitai-compare/sitemap.xml
          if git diff --cached --quiet; then
            echo 'No confirmed corrections to commit.'
            exit 1
          fi
          git commit -m 'fix: 北日本B誰でも通園の複数条件欠落を補正 [skip ci]'
          git pull --rebase origin "${{ github.head_ref }}"
          git push origin HEAD:"${{ github.head_ref }}"
