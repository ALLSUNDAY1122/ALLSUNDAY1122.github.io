name: APP2-009 Codemagic Diagnose

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  diagnose:
    if: github.actor == 'ALLSUNDAY1122' && github.event.issue.title == '[APP2-009] Diagnose Codemagic build'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch and sanitize Codemagic actions
        env:
          CM_API_TOKEN: ${{ secrets.CM_API_TOKEN }}
        run: |
          python - <<'PY'
          import json, os, urllib.request, urllib.error
          from pathlib import Path
          build_id='6a85502ac8a86e02a2bd0e49'
          token=os.environ['CM_API_TOKEN'].strip()
          req=urllib.request.Request(f'https://codemagic.io/api/v3/builds/{build_id}/actions',headers={'x-auth-token':token,'Accept':'application/json'},method='GET')
          try:
              with urllib.request.urlopen(req,timeout=30) as r:
                  payload=json.loads(r.read().decode('utf-8'))
          except urllib.error.HTTPError as e:
              payload={'http_status':e.code,'error':e.read().decode('utf-8',errors='replace')[:4000]}
          sensitive=('secret','token','password','credential','private','apikey','api_key','environment','variable')
          def clean(v):
              if isinstance(v,dict):
                  return {k:('[REDACTED]' if any(p in str(k).lower() for p in sensitive) else clean(x)) for k,x in v.items()}
              if isinstance(v,list): return [clean(x) for x in v]
              return v
          out=Path('automation/codemagic-results/app2-009-kangoshi-testflight-20260819-actions.json')
          out.parent.mkdir(parents=True,exist_ok=True)
          out.write_text(json.dumps(clean(payload),ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          PY
      - name: Persist diagnostic
        run: |
          cp automation/codemagic-results/app2-009-kangoshi-testflight-20260819-actions.json /tmp/app2-009-actions.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin main
          git reset --hard origin/main
          mkdir -p automation/codemagic-results
          cp /tmp/app2-009-actions.json automation/codemagic-results/app2-009-kangoshi-testflight-20260819-actions.json
          git add automation/codemagic-results/app2-009-kangoshi-testflight-20260819-actions.json
          git commit -m 'APP2-009: persist Codemagic pre-start diagnostics'
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Report
        if: always()
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.issues.createComment({owner:context.repo.owner,repo:context.repo.repo,issue_number:context.issue.number,body:`APP2-009 Codemagic diagnostic finished. status=${{ job.status }}; run_id=${context.runId}`});
