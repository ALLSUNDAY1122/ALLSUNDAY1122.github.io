name: Moises L4 W57 Recovery Once (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Recovery workflow retired
        run: |
          echo 'W57 source mutation inside CI is retired.'
          echo 'Use moises-l4-w57-compile-probe.yml for immutable verification of committed source.'
