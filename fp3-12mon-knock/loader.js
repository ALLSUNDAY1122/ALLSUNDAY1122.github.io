'use strict';window.QUESTIONS_READY=(async()=>{const b64=window.QDATA||'';if(!b64)throw new Error('問題データが見つかりません。ページを再読み込みしてください。');if(typeof DecompressionStream==='undefined')throw new Error('このブラウザはデータ展開に対応していません。Safariを最新版に更新してください。');const bytes=Uint8Array.from(atob(b64),c=>c.charCodeAt(0));const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const data=JSON.parse(await new Response(stream).text());if(!Array.isArray(data)||data.length!==600)throw new Error('問題データが不完全です。');return data;})();