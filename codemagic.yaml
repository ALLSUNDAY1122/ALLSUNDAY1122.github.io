workflows:
  otsu4-ios:
    name: 危険物乙4 - Codemagic Mac initialization probe
    max_build_duration: 10
    instance_type: mac_mini_m2
    environment:
      xcode: latest
    scripts:
      - name: Probe Mac workflow
        script: |
          set -euo pipefail
          echo "Codemagic Mac workflow initialized"
          xcodebuild -version
