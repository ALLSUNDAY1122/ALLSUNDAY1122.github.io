istory array storage function',typeof historyGet==='function');ok('combo stat exists',!!els.comboStat);ok('OSM attribution',document.documentElement.innerHTML.includes('OpenStreetMap contributors'));ok('score cap',1800+2700+500===5000);els.selftest.style.display='block';els.selftest.textContent=checks.join('\n')+`\n${checks.every(x=>x.startsWith('PASS'))?'ALL PASS':'FAILED'}`;document.querySelectorAll('.screen').forEach(x=>x.style.display='none');document.body.dataset.test=checks.every(x=>x.startsWith('PASS'))?'pass':'fail'}
  els.startBtn.addEventListener('click',startGame);els.replayBtn.addEventListener('click',startGame);els.homeBtn.addEventListener('click',home);els.summaryHomeBtn.addEventListener('click',home);els.clearHistoryBtn.addEventListener('click',clearHistory);els.submitBtn.addEventListener('click',submit);els.nextBtn.addEventListener('click',nextRound);els.answerRange.addEventListener('input',syncFromRange);els.answerInput.addEventListener('input',syncFromInput);els.answerInput.addEventListener('keydown',e=>{if(e.key==='Enter')submit()});renderBest();renderHistory();window.addEventListener('resize',()=>{if(state.problem&&state.phase!=='intro')drawVisual()});const q=new URLSearchParams(location.search);if(q.get('selftest')==='1')selfTest();else if(q.get('demo'))setTimeout(()=>demo(q.get('demo'),q.get('type')||'cos'),100)
})();
</script>
</body>
</html>
