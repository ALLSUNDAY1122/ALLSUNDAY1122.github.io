ttribution.style.display='block';els.visualTypePill.textContent='REAL MAP';els.areaPill.textContent='東京実地図';const imgs=[...host.querySelectorAll('img[data-map-token]')],check=()=>{if(token!==mapRenderToken)return;if(loaded>0)els.tileWarning.classList.remove('show');else if(failed===tileCount)els.tileWarning.classList.add('show')};imgs.forEach(img=>{img.addEventListener('load',()=>{loaded++;check()},{once:true});img.addEventListener('error',()=>{failed++;check()},{once:true})});setTimeout(()=>{if(token===mapRenderToken&&loaded===0)els.tileWarning.classList.add('show')},5000)
  }
  function drawTanScene(p){mapRenderToken++;els.tileWarning.classList.remove('show');els.mapAttribution.style.display='none';els.visualTypePill.textContent='HEIGHT';els.areaPill.textContent=p.name;const h=els.visual.clientHeight||330,w=els.visual.clientWidth||536,baseX=w*.13,baseY=h*.72,towerX=w*.66,towerTop=h*.18,dx=towerX-baseX,dy=towerTop-baseY,len=Math.hypot(dx,dy),angle=deg(Math.atan2(-dy,dx)),heightPx=baseY-towerTop;els.visual.innerHTML=`<div class="scene"><div class="scene-city"></div><div class="scene-ground"></div><div class="tower-shape" style="height:${heightPx}px"></div><div class="tower-tip" style="bottom:${h-baseY+heightPx}px;height:28px"></div><div class="observer"></div><div class="baseline" style="left:${baseX}px;top:${baseY}px;width:${dx}px"></div><div class="sightline" style="left:${baseX}px;top:${baseY}px;width:${len}px;transform:rotate(${-angle}deg)"></div><div class="scene-label" style="left:${baseX+dx*.38}px;top:${baseY+8}px">${p.type==='tan_height'?`${p.distance} m`:'? m'}</div><div class="scene-label" style="left:${baseX+35}px;top:${baseY-54}px">${Math.round(p.angle)}°</div><div class="scene-label" style="left:${towerX-25}px;top:${towerTop-34}px">${p.type==='tan_distance'?`${p.height} m`:(state.phase==='result'?`${p.answer} m`:'? m')}</div></div>`}
  function drawVisual(){if(state.problem.type.startsWith('tan'))drawTanScene(state.problem);else drawMap(state.problem)}
  function known(label,value,wide=false){return`<div class="known${wide?' wide':''}"><span>${label}</span><b>${value}</b></div>`}
  function updateConditions(){const p=state.problem;els.routeTitle.textContent=p.title;els.typeBadge.textContent=p.type==='cos'?'COS':p.type==='sin'?'SIN':'TAN';els.questionText.textContent=`${p.answerLabel}を求めてください。`;if(p.type==='cos'){els.knownGrid.innerHTML=known(`A ${p.A.name} → B ${p.B.name}`,`${fmt(p.ab)} km`)+known(`A ${p.A.name} → C ${p.C.name}`,`${fmt(p.ac)} km`)+known(`∠BAC（${p.A.name}）`,`${Math.round(p.angleA)}°`,true);els.routeKey.innerHTML='<div class="key"><i class="line"></i>既知の2辺</div><div class="key"><i class="line unknown"></i>求める辺</div>'}
    else if(p.type==='sin'){els.knownGrid.innerHTML=known(`既知の辺 A–B`,`${fmt(p.ab)} km`)+known(`∠BAC（A）`,`${Math.round(p.angleA)}°`)+known(`∠ACB（C）`,`${Math.round(p.angleC)}°`,true);els.routeKey.innerHTML='<div class="key"><i class="line"></i>既知の辺</div><div class="key"><i class="line guide"></i>補助辺</div><div class="key"><i class="line unknown"></i>求める辺</div>'}
    else if(p.type==='tan_height'){els.knownGrid.innerHTML=known('対象までの水平距離',`${p.distance} m`)+known('見上げ角度',`${Math.round(p.angle)}°`)+known('求めるもの','建物の高さ',true);els.routeKey.innerHTML='<div class="key"><i class="line"></i>水平距離</div><div class="key"><i class="line unknown"></i>視線</div>'}
    else{els.knownGrid.innerHTML=known('建物の高さ',`${p.height} m`)+known('見上げ角度',`${Math.round(p.angle)}°`)+known('求めるもの','建物までの水平距離',true);els.routeKey.innerHTML='<div class="key"><i class="line"></i>求める水平距離</div><div class="key"><i class="line unknown"></i>視線</div>'}
    const max=p.unit==='km'?Math.max(5,Math.ceil(p.answer*2.2)):Math.max(200,Math.ceil(p.answer*2.1/50)*50),step=p.unit==='km'?.1:1,initial=p.unit==='km'?Math.max(.1,Math.round(p.answer*.72*10)/10):Math.max(1,Math.round(p.answer*.72));els.answerRange.min=p.unit==='km'?.1:1;els.answerRange.max=max;els.answerRange.step=step;els.answerInput.min=els.answerRange.min;els.answerInput.max=max;els.answerInput.step=step;els.answerRange.value=initial;els.answerInput.value=initial;els.answerUnit.textContent=p.unit;els.rangeHint.textContent=`${els.answerRange.min}〜${max}${p.unit}`
  }
  function startTimer(){clearInterval(state.timer);state.startedAt=Date.now();els.timerText.textContent='0秒';state.timer=setInterval(()=>els.timerText.textContent=`${Math.floor((Date.now()-state.startedAt)/1000)}秒`,500)}function elapsed(){return Math.max(1,(Date.now()-state.startedAt)/1000)}
  function startGame(){state.round=0;state.total=0;state.results=[];state.rounds=buildRounds();state.combo=0;state.bestCombo=0;show('game');loadRound()}
  function loadRound(){state.phase='estimate';state.estimate=null;state.calc=null;state.problem=state.rounds[state.round];els.roundStat.textContent=`${state.round+1} / ${state.rounds.length}`;els.scoreStat.textContent=num(state.total);els.progressBar.style.width=`${((state.round+1)/state.rounds.length)*100}%`;els.phaseLabel.className='phase';els.phaseLabel.textContent='概算フェーズ';els.instruction.textContent='表示された条件から、まずはおおよその答えを入力してください。';els.formula.classList.remove('show');els.resultPanel.classList.remove('show');els.answerCard.style.display='block';els.submitBtn.textContent='概算を決定';updateConditions();drawVisual();startTimer()}
  function readAnswer(){const v=Number(els.answerInput.value);return Number.isFinite(v)&&v>0?v:null}function syncFromRange(){els.answerInput.value=Number(els.answerRange.value).toFixed(state.problem&&state.problem.unit==='km'?1:0)}function syncFromInput(){const v=Number(els.answerInput.value);if(Number.isFinite(v))els.answerRange.value=Math.min(Number(els.answerRange.max),Math.max(Number(els.answerRange.min),v))}
  function accuracy(answer,correct,max,power=1.3){const e=Math.abs(answer-correct)/correct;return Math.max(0,Math.round(max*(1-Math.min(1,e))**power))}
  function submit(){const v=readAnswer();if(v===null){els.answerInput.focus();return}if(state.phase==='estimate')submitEstimate(v);else if(state.phase==='calc')submitCalc(v)}
  function trigValue(type,angle){const val=type==='cos'?Math.cos(rad(angle)):type==='sin'?Math.sin(rad(angle)):Math.tan(rad(angle));return val.toFixed(2)}
  function submitEstimate(v){state.estimate=v;state.phase='calc';clearInterval(state.timer);els.phaseLabel.className='phase calc';els.phaseLabel.textContent='計算フェーズ';els.instruction.textContent=`最初の予想は ${fmt(v,state.problem.unit)} ${state.problem.unit}。代入ヒントを使って答えを補正します。`;const p=state.problem;els.formula.classList.add('show');
    if(p.type==='cos'){const c=trigValue('cos',p.angleA),inside=p.ab*p.ab+p.ac*p.ac-2*p.ab*p.ac*Number(c);els.formulaTitle.textContent='余弦定理に代入';els.formulaText.textContent=`BC = √(${fmt(p.ab)}² + ${fmt(p.ac)}² − 2 × ${fmt(p.ab)} × ${fmt(p.ac)} × cos ${Math.round(p.angleA)}°)`;els.trigHint.innerHTML=`三角比の概算：<b>cos ${Math.round(p.angleA)}° ≈ ${c}</b><br>したがって、ルートの中はおよそ <b>${inside.toFixed(1)}</b> です。`}
    else if(p.type==='sin'){const sa=trigValue('sin',p.angleA),sc=trigValue('sin',p.angleC