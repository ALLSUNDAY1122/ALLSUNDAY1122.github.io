);els.formulaTitle.textContent='正弦定理に代入';els.formulaText.textContent=`BC = AB × sin A ÷ sin C = ${fmt(p.ab)} × sin ${Math.round(p.angleA)}° ÷ sin ${Math.round(p.angleC)}°`;els.trigHint.innerHTML=`三角比の概算：<b>sin ${Math.round(p.angleA)}° ≈ ${sa}</b>、<b>sin ${Math.round(p.angleC)}° ≈ ${sc}</b><br>${fmt(p.ab)} × ${sa} ÷ ${sc} と考えます。`}
    else{const t=trigValue('tan',p.angle);els.formulaTitle.textContent='tanに代入';if(p.type==='tan_height'){els.formulaText.textContent=`高さ = 水平距離 × tan 角度 = ${p.distance} × tan ${Math.round(p.angle)}°`;els.trigHint.innerHTML=`三角比の概算：<b>tan ${Math.round(p.angle)}° ≈ ${t}</b><br>${p.distance} × ${t} と考えます。`}else{els.formulaText.textContent=`水平距離 = 高さ ÷ tan 角度 = ${p.height} ÷ tan ${Math.round(p.angle)}°`;els.trigHint.innerHTML=`三角比の概算：<b>tan ${Math.round(p.angle)}° ≈ ${t}</b><br>${p.height} ÷ ${t} と考えます。`}}
    els.submitBtn.textContent='計算後の答えを決定';drawVisual();startTimer()
  }
  function submitCalc(v){state.calc=v;const p=state.problem,t=elapsed();clearInterval(state.timer);const estErr=Math.abs(state.estimate-p.answer),calcErr=Math.abs(v-p.answer);const improved=calcErr<estErr;state.combo=improved?state.combo+1:0;state.bestCombo=Math.max(state.bestCombo,state.combo);const estScore=accuracy(state.estimate,p.answer,1800,1.15),calcScore=accuracy(v,p.answer,2700,1.5),speedScore=Math.max(0,Math.round(500-Math.max(0,t-12)*11)),roundScore=estScore+calcScore+speedScore,result={...p,estimate:state.estimate,calc:v,estScore,calcScore,speedScore,roundScore,combo:state.combo,improved};state.results.push(result);state.total+=roundScore;state.phase='result';els.comboStat.textContent=`×${state.combo}`;drawVisual();renderResult(result)}
  function resultMessage(r){const estErr=Math.abs(r.estimate-r.answer),calcErr=Math.abs(r.calc-r.answer),tol=Math.max(r.unit==='km'?.01:1,r.answer*.0005);if(Math.abs(estErr-calcErr)<=tol)return`概算と計算後は<br><b>同じ精度</b>でした`;if(calcErr<estErr)return`数学で<br><b>${fmt(estErr-calcErr,r.unit)}${r.unit}</b> 近づいた`;return`今回は概算の方が<br><b>${fmt(calcErr-estErr,r.unit)}${r.unit}</b> 正解に近かった`}
  function renderResult(r){els.answerCard.style.display='none';els.resultPanel.classList.add('show');els.phaseLabel.className='phase result';els.phaseLabel.textContent='結果';els.questionText.textContent=`正解は ${fmt(r.answer,r.unit)} ${r.unit}`;els.roundScore.textContent=num(r.roundScore);els.estimateScore.textContent=num(r.estScore);els.calcScore.textContent=num(r.calcScore);els.speedScore.textContent=num(r.speedScore);els.resultMsg.innerHTML=resultMessage(r);const max=Math.max(r.estimate,r.calc,r.answer)*1.08,rows=[['概算',r.estimate,'est'],['計算後',r.calc,'calc'],['正解',r.answer,'correct']];els.bars.innerHTML=rows.map(([label,v,cls])=>`<div class="bar-row"><b>${label}</b><div class="track"><i class="${cls}" style="width:${Math.max(3,v/max*100)}%"></i></div><div class="value">${fmt(v,r.unit)}${r.unit}</div></div>`).join('');els.lesson.innerHTML=(r.improved?`<b>COMBO ×${r.combo}</b> 継続中。 `:`今回コンボは途切れました。 `)+(r.type==='cos'?`余弦定理は、<b>2辺とその間の角度</b>から残りの1辺を求めます。`:r.type==='sin'?`正弦定理は、<b>辺とその向かいの角度</b>の比が共通であることを使います。`:`tanは、<b>高さ ÷ 水平距離</b>を見上げ角度と結びつけます。`);els.nextBtn.textContent=state.round===state.rounds.length-1?'最終結果を見る':'次の問題へ';els.scoreStat.textContent=num(state.total)}
  function nextRound(){if(state.round===state.rounds.length-1){finish();return}state.round++;loadRound()}
  const memoryStore={};function storageGet(k){try{const v=localStorage.getItem(k);return v===null?(memoryStore[k]??null):v}catch(_){return memoryStore[k]??null}}function storageSet(k,v){memoryStore[k]=v;try{localStorage.setItem(k,v)}catch(_){}}
  function historyGet(){try{const x=JSON.parse(storageGet('hakaruHistory3RoundV1')||'[]');return Array.isArray(x)?x:[]}catch(_){return[]}}
  function gradeFor(score){const rate=score/15000;return rate>=.9?'測量マスター':rate>=.75?'距離感覚 上級':rate>=.55?'距離感覚 中級':'三角比を練習中'}
  function saveHistory(){const h=historyGet(),counts=state.results.reduce((o,r)=>(o[r.type.startsWith('tan')?'tan':r.type]++,o),{cos:0,sin:0,tan:0});h.unshift({at:new Date().toISOString(),score:state.total,grade:gradeFor(state.total),counts,bestCombo:state.bestCombo});storageSet('hakaruHistory3RoundV1',JSON.stringify(h.slice(0,20)))}
  function renderHistory(){const h=historyGet(),html=h.length?h.slice(0,10).map(x=>{const d=new Date(x.at),date=new Intl.DateTimeFormat('ja-JP',{timeZone:'Asia/Tokyo',month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}).format(d);return`<div class="history-item"><div><b>${x.grade}</b><span>${date} ／ cos ${x.counts.cos}・sin ${x.counts.sin}・tan ${x.counts.tan} ／ best combo ×${x.bestCombo||0}</span></div><strong>${num(x.score)}</strong></div>`}).join(''):'<div class="history-empty">まだ得点履歴がありません。</div>';els.historyIntro.innerHTML=html;els.historySummary.innerHTML=html}
  function finish(){clearInterval(state.timer);const best=Math.max(Number(storageGet('hakaruBest3RoundV1')||0),state.total);storageSet('hakaruBest3RoundV1',best);saveHistory();renderBest();renderHistory();els.totalScore.textContent=num(state.total);els.grade.textContent=gradeFor(state.total);els.roundList.innerHTML=state.results.map((r,i)=>`<div class="round-item"><div><b>${i+1}. ${r.type==='cos'?'COS':r.type==='sin'?'SIN':'TAN'}｜${r.title}</b><br>正解 ${fmt(r.answer,r.unit)}${r.unit} ／ 計算後 ${fmt(r.calc,r.unit)}${r.unit}<br><span style="color:#8fb4de">${r.improved?`COMBO ×${r.combo} 継続`:`コンボ更新なし`}</span></div><strong>${num(r.roundScore)}</strong></div>`).join('');show('summary')}
  function renderBest(){const b=Number(storageGet('hakaruBest3RoundV1')||0);els.bestStat.textContent=b?num(b):'—'}function home(){clearInterval(state.timer);state.phase='intro';renderHistory();show('intro')}
  function clearHistory(){if(!confirm('得点履歴を削除しますか？'))return;storageSet('hakaruHistory3RoundV1','[]');renderHistory()}
  function demo(mode,type='cos'){const p=type==='sin'?makeSin(SIN_SET[0]):type==='tan'?makeTan(TAN_SET[0]):makeCos(COS_SET[0]);state.rounds=[p,p,p];state.round=0;state.total=0;state.results=[];show('game');loadRound();if(mode==='calc')submitEstimate(p.answer*.8);if(mode==='result'){submitEstimate(p.answer*.9);submitCalc(p.answer*.9)}}
  function selfTest(){const checks=[],ok=(name,cond)=>checks.push(`${cond?'PASS':'FAIL'} ${name}`),c=makeCos(COS_SET[0]),s=makeSin(SIN_SET[0]),t=makeTan(TAN_SET[0]);ok('cosine law',Math.abs(Math.sqrt(c.ab*c.ab+c.ac*c.ac-2*c.ab*c.ac*Math.cos(rad(c.angleA)))-c.answer)<.05);ok('sine law',Math.abs(s.ab*Math.sin(rad(s.angleA))/Math.sin(rad(s.angleC))-s.answer)<.05);ok('tangent height',Math.abs(t.distance*Math.tan(rad(t.angle))-t.answer)<.5);ok('mixed three rounds',buildRounds().length===3);ok('all trig families included',(()=>{const z=buildRounds().map(x=>x.type.startsWith('tan')?'tan':x.type);return['cos','sin','tan'].every(x=>z.includes(x))})());ok('angle labels use visible navy text',angleArc({x:0,y:0},{x:20,y:0},{x:0,y:20},'90°').includes('fill="#12203a"'));ok('equal message handled',resultMessage({estimate:10,calc:10,answer:10.7,unit:'km'}).includes('同じ精度'));ok('h