lat-a.lat),dl=rad(b.lon-a.lon);const q=Math.sin(dp/2)**2+Math.cos(rad(a.lat))*Math.cos(rad(b.lat))*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}
  function bearing(a,b){const y=Math.sin(rad(b.lon-a.lon))*Math.cos(rad(b.lat));const x=Math.cos(rad(a.lat))*Math.sin(rad(b.lat))-Math.sin(rad(a.lat))*Math.cos(rad(b.lat))*Math.cos(rad(b.lon-a.lon));return(deg(Math.atan2(y,x))+360)%360}
  function angleAt(a,b,c){let d=Math.abs(bearing(a,b)-bearing(a,c));return d>180?360-d:d}
  function shuffle(a){const x=[...a];for(let i=x.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[x[i],x[j]]=[x[j],x[i]]}return x}
  function fmt(n,unit='km'){const d=unit==='m'?0:(n>=10?1:2);return Number(n).toFixed(d)}function num(n){return Math.round(n).toLocaleString('ja-JP')}
  function show(id){['intro','game','summary'].forEach(x=>els[x].classList.toggle('active',x===id))}
  function makeCos(keys){const[A,B,C]=keys.map(k=>PLACES[k]);return{type:'cos',A,B,C,ab:distance(A,B),ac:distance(A,C),answer:distance(B,C),angleA:angleAt(A,B,C),unit:'km',title:`${B.name} – ${C.name}`,answerLabel:'B–Cの距離'}}
  function makeSin(keys){const[A,B,C]=keys.map(k=>PLACES[k]);return{type:'sin',A,B,C,ab:distance(A,B),answer:distance(B,C),angleA:angleAt(A,B,C),angleC:angleAt(C,A,B),unit:'km',title:`${B.name} – ${C.name}`,answerLabel:'B–Cの距離'}}
  function makeTan(x){const angle=deg(Math.atan(x.height/x.distance));return{type:x.kind==='height'?'tan_height':'tan_distance',name:x.name,height:x.height,distance:x.distance,angle,answer:x.kind==='height'?x.height:x.distance,unit:'m',title:x.kind==='height'?`${x.name}の高さ`:`${x.name}までの距離`,answerLabel:x.kind==='height'?'建物の高さ':'水平距離'}}
  function buildRounds(){const cos=shuffle(COS_SET),sin=shuffle(SIN_SET),tan=shuffle(TAN_SET);return shuffle([makeCos(cos[0]),makeSin(sin[0]),makeTan(tan[0])])}
  function mercatorWorld(lat,lon,z){const n=2**z,x=(lon+180)/360*n*256;const c=Math.max(-85.05112878,Math.min(85.05112878,lat));const y=(1-Math.log(Math.tan(rad(c))+1/Math.cos(rad(c)))/Math.PI)/2*n*256;return{x,y}}
  function chooseZoom(points,w,h){for(let z=15;z>=8;z--){const px=points.map(p=>mercatorWorld(p.lat,p.lon,z));const bw=Math.max(...px.map(p=>p.x))-Math.min(...px.map(p=>p.x));const bh=Math.max(...px.map(p=>p.y))-Math.min(...px.map(p=>p.y));if(bw<=w*.68&&bh<=h*.62)return z}return 8}
  function svgLabel(x,y,text,kind='edge'){const width=Math.max(52,Math.min(190,text.length*7.2+18));const bg=kind==='angle'?'#12203a':'rgba(255,255,255,.97)';const border=kind==='unknown'?'#ed7a18':kind==='angle'?'#fff':'#1d64e8';const textColor=kind==='angle'?'#fff':kind==='unknown'?'#9d4e0b':'#164da6';return`<g transform="translate(${(x-width/2).toFixed(1)},${(y-13).toFixed(1)})"><rect width="${width}" height="26" rx="13" fill="${bg}" stroke="${border}" stroke-width="2"/><text x="${width/2}" y="17" text-anchor="middle" font-size="11" font-weight="900" fill="${textColor}" font-family="-apple-system,BlinkMacSystemFont,'Hiragino Sans','Yu Gothic',sans-serif">${text}</text></g>`}
  function pointGroup(pt,letter,kind,name,dx,dy){const color=kind==='a'?'#12203a':kind==='b'?'#1d64e8':'#16a6b6';const labelW=Math.max(62,Math.min(160,name.length*12+18));const lx=pt.x+dx,ly=pt.y+dy;return`<g><line x1="${pt.x}" y1="${pt.y}" x2="${lx}" y2="${ly}" stroke="#667085" stroke-width="1.2"/><circle cx="${pt.x}" cy="${pt.y}" r="15" fill="${color}" stroke="#fff" stroke-width="4"/><text x="${pt.x}" y="${pt.y+4}" text-anchor="middle" font-size="12" font-weight="950" fill="#fff">${letter}</text><g transform="translate(${lx-labelW/2},${ly-12})"><rect width="${labelW}" height="24" rx="8" fill="rgba(255,255,255,.97)" stroke="#cdd8e7"/><text x="${labelW/2}" y="16" text-anchor="middle" font-size="11" font-weight="900" fill="#172033">${name}</text></g></g>`}
  function angleArc(V,P1,P2,label,offset=0){let a1=Math.atan2(P1.y-V.y,P1.x-V.x),a2=Math.atan2(P2.y-V.y,P2.x-V.x);let d=((a2-a1+Math.PI*3)%(Math.PI*2))-Math.PI;const r=26,sx=V.x+Math.cos(a1)*r,sy=V.y+Math.sin(a1)*r,ex=V.x+Math.cos(a1+d)*r,ey=V.y+Math.sin(a1+d)*r,large=Math.abs(d)>Math.PI?1:0,sweep=d>0?1:0,mid=a1+d/2,lx=V.x+Math.cos(mid)*(38+offset),ly=V.y+Math.sin(mid)*(38+offset);return`<path d="M ${sx} ${sy} A ${r} ${r} 0 ${large} ${sweep} ${ex} ${ey}" fill="none" stroke="#12203a" stroke-width="5" stroke-linecap="round"/><text x="${lx}" y="${ly+4}" text-anchor="middle" font-size="12" font-weight="950" fill="#12203a" stroke="#fff" stroke-width="4" paint-order="stroke" font-family="-apple-system,BlinkMacSystemFont,'Hiragino Sans','Yu Gothic',sans-serif">${label}</text>`}
  function drawMap(p){const host=els.visual,w=host.clientWidth||536,h=host.clientHeight||330,z=chooseZoom([p.A,p.B,p.C],w,h),world=[p.A,p.B,p.C].map(q=>mercatorWorld(q.lat,q.lon,z)),cx=world.reduce((s,q)=>s+q.x,0)/3,cy=world.reduce((s,q)=>s+q.y,0)/3,left=cx-w/2,top=cy-h/2,screen=world.map(q=>({x:q.x-left,y:q.y-top})),tileX0=Math.floor(left/256),tileX1=Math.floor((left+w)/256),tileY0=Math.floor(top/256),tileY1=Math.floor((top+h)/256),n=2**z,token=++mapRenderToken;let tiles='',tileCount=0,failed=0,loaded=0;for(let ty=tileY0;ty<=tileY1;ty++)for(let tx=tileX0;tx<=tileX1;tx++){const wrapped=((tx%n)+n)%n,px=tx*256-left,py=ty*256-top;tiles+=`<img class="map-tile" alt="" draggable="false" data-map-token="${token}" src="https://tile.openstreetmap.org/${z}/${wrapped}/${ty}.png" style="left:${px}px;top:${py}px">`;tileCount++}
    const[A,B,C]=screen,edge=(u,v,color,dash='')=>`<line x1="${u.x}" y1="${u.y}" x2="${v.x}" y2="${v.y}" stroke="rgba(255,255,255,.92)" stroke-width="9" stroke-linecap="round"/><line x1="${u.x}" y1="${u.y}" x2="${v.x}" y2="${v.y}" stroke="${color}" stroke-width="5" stroke-linecap="round" ${dash?`stroke-dasharray="${dash}"`:''}/>`;const mid=(u,v,t=.5)=>({x:u.x+(v.x-u.x)*t,y:u.y+(v.y-u.y)*t}),centroid={x:(A.x+B.x+C.x)/3,y:(A.y+B.y+C.y)/3},off=pt=>{const dx=pt.x-centroid.x,dy=pt.y-centroid.y,len=Math.hypot(dx,dy)||1;return[dx/len*50,dy/len*40]},[adx,ady]=off(A),[bdx,bdy]=off(B),[cdx,cdy]=off(C);let geometry='';
    if(p.type==='cos'){geometry=edge(A,B,'#1d64e8')+edge(A,C,'#1d64e8')+edge(B,C,'#ed7a18','10 9')+svgLabel(mid(A,B).x,mid(A,B).y-13,`${fmt(p.ab)} km`)+svgLabel(mid(A,C).x,mid(A,C).y+15,`${fmt(p.ac)} km`)+svgLabel(mid(B,C).x,mid(B,C).y-14,state.phase==='result'?`${fmt(p.answer)} km`:'? km','unknown')+angleArc(A,B,C,`${Math.round(p.angleA)}°`)}
    else{geometry=edge(A,B,'#1d64e8')+edge(A,C,'#7d8798','4 7')+edge(B,C,'#ed7a18','10 9')+svgLabel(mid(A,B).x,mid(A,B).y-13,`${fmt(p.ab)} km`)+svgLabel(mid(B,C,.68).x,mid(B,C,.68).y-10,state.phase==='result'?`${fmt(p.answer)} km`:'? km','unknown')+angleArc(A,B,C,`${Math.round(p.angleA)}°`)+angleArc(C,A,B,`${Math.round(p.angleC)}°`,6)}
    const svg=`<svg class="map-svg" viewBox="0 0 ${w} ${h}" aria-label="実地図上の三角形"><g>${geometry}${pointGroup(A,'A','a',p.A.name,adx,ady)}${pointGroup(B,'B','b',p.B.name,bdx,bdy)}${pointGroup(C,'C','c',p.C.name,cdx,cdy)}</g></svg>`;host.innerHTML=tiles+'<div class="map-shade"></div>'+svg;els.mapA